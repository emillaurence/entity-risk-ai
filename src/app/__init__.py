"""
src.app — Streamlit application layer.

Modules
-------
factory     Wire and cache all system components (AI, DB, agents, orchestrator).
state       Typed accessors and mutators for st.session_state.
layout      Three-column page skeleton (left / center / right).
components  Individual UI component renderers called from layout.
"""
